; The app caps uploads at 10 MB (apps/api/config/materials.php). These two sit
; ABOVE that cap deliberately: the request must reach Laravel so validation
; answers with a 422 the SPA can render under the file input, instead of PHP
; rejecting the body first and producing a bare 413.
upload_max_filesize = 12M
post_max_size = 16M
