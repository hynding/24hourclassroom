import { Component, h } from '@stencil/core';

/** Stub: plan 4 task 5 replaces this with the real page. */
@Component({ tag: 'page-integrations', shadow: true })
export class PageIntegrations {
  render() {
    return (
      <section>
        <h1>Integrations</h1>
      </section>
    );
  }
}
