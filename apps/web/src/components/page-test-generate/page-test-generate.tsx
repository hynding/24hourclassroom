import { Component, h } from '@stencil/core';

/** Stub: plan 4 task 6 replaces this with the real page. */
@Component({ tag: 'page-test-generate', shadow: true })
export class PageTestGenerate {
  render() {
    return (
      <section>
        <h1>Generate a test</h1>
      </section>
    );
  }
}
